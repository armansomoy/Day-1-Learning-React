const person = {
    name: 'red',
    prefession: 'police',
    age: '23',
    25: 'id',
    address: 'jurain'

}

// dot notation 
const prof1 = person.prefession;

// bracket notation 
const prof2 = person["prefession"];
const pName = 'proefession';
const prof3 = person[pName];

const idNum = person[25];
console.log(idNum)
console.log(prof2);